name: Keep Streamlit App Awake

on:
  schedule:
    - cron: "0 */11 * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Streamlit App
        run: |
          curl -I https://options-pricing-dashboard.streamlit.app/